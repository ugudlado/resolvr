# Prefer Existing Libraries Over Custom Code

During design and spec writing, search for well-maintained libraries (via Context7, npm, web search) that solve the problem before proposing custom implementations.

Only build from scratch when no suitable library exists or when the library would add disproportionate complexity.

Document the library search and decision in the spec's "Alternatives Considered" section.
