# Spec-First Development

Every feature needs a spec (`specs/active/spec.md`) before implementation begins. No code without a spec.

This applies to new features, refactors, and any non-trivial change. Quick bug fixes and typo corrections are exempt.
