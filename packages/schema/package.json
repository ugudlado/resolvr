{
  "name": "@todos/schema",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run --exclude '**/e2e/**' --exclude '**/integration/**'",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "clean": "rm -rf dist tsconfig.build.tsbuildinfo",
    "build": "node esbuild.config.js && tsc --build tsconfig.build.json --emitDeclarationOnly",
    "rebuild": "pnpm clean && pnpm build",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:migrate:drop": "drizzle-kit drop",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:check": "drizzle-kit check"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.2.3",
    "@vitest/ui": "^3.2.3",
    "drizzle-kit": "^0.30.6",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.3",
    "vitest": "^3.2.3"
  },
  "dependencies": {
    "@neondatabase/serverless": "github:neondatabase/serverless",
    "drizzle-orm": "^0.39.3",
    "drizzle-zod": "^0.7.1",
    "zod": "^3.25.76",
    "postgres": "^3.4.7"
  }
}
